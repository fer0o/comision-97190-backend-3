# AdoptMe API

API backend para gestion de adopciones de mascotas.

## Estado

Base inicial del servidor Express con endpoint de salud.

```bash
npm install
npm run dev
```

Endpoint base:

```txt
GET /api/health
```
